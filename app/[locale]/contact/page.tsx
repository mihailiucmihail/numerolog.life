import { Footer } from "@/components/footer"
import { Navbar } from "@/components/navbar"
import { StarField } from "@/components/star-field"
import { Mail, Clock } from "lucide-react"
import { Button } from "@/components/ui/button"
export const dynamic = "force-dynamic"
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; return locale === "ru" ? { title: "Контакты | NUMEROLOG.life", description: "Свяжитесь с командой NUMEROLOG.life" } : { title: "Contact | NUMEROLOG.life", description: "Contactați echipa NUMEROLOG.life" } }
export default async function ContactPage({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; const ru=locale === "ru"; return <main className="relative min-h-screen bg-background"><StarField/><Navbar/><div className="relative z-10 mx-auto max-w-4xl px-6 pb-20 pt-32 sm:px-8 lg:px-12"><div className="mb-16 text-center"><h1 className="mb-6 font-serif text-4xl font-medium text-gradient sm:text-5xl">{ru ? "Свяжитесь с нами" : "Contactează-ne"}</h1><p className="mx-auto max-w-2xl text-lg text-muted-foreground/80">{ru ? "Мы заботимся о нашем сообществе. Напишите нам, и мы ответим как можно скорее." : "Avem grijă de comunitatea noastră. Scrie-ne și îți vom răspunde cât mai curând."}</p></div><div className="mx-auto mb-16 grid max-w-xl gap-6 sm:grid-cols-2"><div className="glass-card rounded-2xl p-6 text-center"><Mail className="mx-auto mb-4 h-8 w-8 text-primary"/><h3 className="mb-2 font-serif font-medium">Email</h3><p className="text-sm text-muted-foreground/80">contact@numerolog.life</p></div><div className="glass-card rounded-2xl p-6 text-center"><Clock className="mx-auto mb-4 h-8 w-8 text-primary"/><h3 className="mb-2 font-serif font-medium">{ru ? "График работы" : "Program"}</h3><p className="text-sm text-muted-foreground/80">{ru ? "Понедельник — пятница: 9:00–18:00" : "Luni - Vineri: 9:00 - 18:00"}</p></div></div><div className="glass-card mx-auto max-w-xl rounded-2xl p-8"><h2 className="mb-6 text-center font-serif text-2xl font-medium">{ru ? "Отправить сообщение" : "Trimite-ne un Mesaj"}</h2><form className="space-y-4"><div><label className="mb-2 block text-sm font-medium">{ru ? "Имя" : "Nume"}</label><input type="text" className="w-full rounded-lg border border-border/50 bg-background/50 px-4 py-3 focus:border-primary/50 focus:outline-none" placeholder={ru ? "Ваше имя" : "Numele tău"}/></div><div><label className="mb-2 block text-sm font-medium">Email</label><input type="email" className="w-full rounded-lg border border-border/50 bg-background/50 px-4 py-3 focus:border-primary/50 focus:outline-none" placeholder="email@example.com"/></div><div><label className="mb-2 block text-sm font-medium">{ru ? "Сообщение" : "Mesaj"}</label><textarea rows={4} className="w-full resize-none rounded-lg border border-border/50 bg-background/50 px-4 py-3 focus:border-primary/50 focus:outline-none" placeholder={ru ? "Ваше сообщение..." : "Mesajul tău..."}/></div><Button className="w-full bg-primary hover:bg-primary/90">{ru ? "Отправить сообщение" : "Trimite mesajul"}</Button></form></div></div><Footer/></main> }
